// deprecato — si rimuove nel lotto 6. `Pill` è `Chip` (ui/Chip) con il nome
// storico: stesse props (`active` = `selected`). I nuovi consumer importano
// `Chip`.
export { Chip as Pill, type ChipProps as PillProps, type ChipShape as PillShape } from "../Chip/Chip";
